// Placeholder route for the center "+" tab. It's never actually shown — the tab's custom
// tabBarButton (see the tabs _layout) opens the quick-action slide-up sheet instead of
// navigating here. Expo Router still needs a file for the registered tab.
export default function CreateActionPlaceholder() {
  return null;
}
